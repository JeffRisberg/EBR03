<div class="container">
    <div class="row">
        <div class="col-md-2">
            Welcome to EBR03
        </div>
        <div class="col-md-7">
            {{#if user}}
                Hello, {{user.name}}
            {{/if}}
            {{#unless user}}
                {{#link-to 'register'}}Register{{/link-to}}&nbsp;
                {{#link-to 'login'}}Login{{/link-to}}
            {{/unless}}
        </div>
        <div class="col-md-3">
            {{#link-to 'configured'}}Configured{{/link-to}}
            {{#link-to 'dashboard'}}Dashboard{{/link-to}}
            {{#link-to 'charities'}}Charities{{/link-to}}
        </div>
    </div>
    <div class="row">
        <div>
            {{outlet}}
        </div>
    </div>
</div>